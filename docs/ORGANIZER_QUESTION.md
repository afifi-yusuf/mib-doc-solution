# Public eligibility question to post

> Is a vision-only ImageNet-pretrained MobileNetV3-Large backbone permissible if it is fine-tuned solely on the 1,000 public training PDFs, packaged entirely inside the offline Docker image, has no text/language input, fits under the 250 MiB per-artifact limit, and is used only for packet classification? The primary submission will remain a custom CNN unless this receives explicit approval.

Do not enable the optional pretrained-backbone path without a public organizer response.
